name: test_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  cupertino_native: ^0.1.1
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  


flutter:
  uses-material-design: true
  fonts:
    - family: Alata
      fonts:
        - asset: assets/Alata/Alata-Regular.ttf
